/**
 * Re-export barrel — redirects to the new modular onboarding system.
 * Kept for backward compatibility with existing imports.
 */
export { GuideBookButton, GuideBookMobileButton } from "@/components/onboarding/GuideBookButton"
export { openOnboardingGuide } from "@/components/onboarding/OnboardingProvider"
